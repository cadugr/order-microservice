# Enable Dev Services for tests
quarkus.datasource.devservices.enabled=true
# Ensure Liquibase runs for tests
quarkus.liquibase.migrate-at-start=true
# Quarkus Dev Services will automatically configure the URL, username, and password
# for the test database. No need to specify them here.